import React from "react";
import { createstore, applyMiddleware } from "redux";
import thunk from "redux-thunk";
import {composeWithDevTool} from "redux-devtools-extention";

function Store() {
  return <>store</>;
}

export default Store;
